# SEP-032: CrossRef + arXiv native APIs, World Bank Indicators provider, cache seeding, background refresh

**Status:** Open
**Created:** 2026-03-06
**Project:** zorora

## Summary
Add CrossRef and arXiv native API as search surfaces, World Bank Indicators as cached time-series provider, seed market_data.db and commit to git, background thread to update cached data on startup

## Motivation
Current academic search uses DDG site:arxiv.org which is unreliable and returns no abstracts. CrossRef (100M+ records) provides structured metadata. Market data caching is reactive-only with no seeded baseline and no background refresh. World Bank Indicators time-series data is absent.

## Proposed Change
1) Add CrossRef backend to academic_search.py, replace arXiv DDG with native API. 2) Add WB Indicators as third provider. 3) Create worldbank_indicators_client.py. 4) Seed market_data.db. 5) Add daemon thread for background refresh.

## Acceptance Criteria
CrossRef and arXiv native return structured results. WB Indicators in SERIES_CATALOG. Seeded DB committed. Background thread updates stale series. All tests pass.

## Commits
<!-- Auto-updated by hook workflow -->
