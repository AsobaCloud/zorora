# SEP-058: Cache market/latest endpoint response

**Status:** Open
**Created:** 2026-03-21
**Project:** zorora

## Summary
Add in-memory cache with 60s TTL to /api/market/latest. Currently runs 160+ SQLite queries per request (80 get_series_df + 80 get_staleness), taking 5s server-side. Data only changes when background threads refresh (every 24h).

## Motivation
Global View tab takes 5s to load because market/latest runs 160 uncached SQLite queries on every request.

## Proposed Change
Add module-level cache dict with timestamp. Return cached response if age < 60s, otherwise recompute.

## Acceptance Criteria
market/latest returns in under 100ms on cache hit. First request still takes 5s, subsequent requests under 100ms for 60 seconds.

## Commits
<!-- Auto-updated by hook workflow -->
